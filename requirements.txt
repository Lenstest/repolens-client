# Core
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.0
aiofiles==23.2.1
httpx==0.27.0
certifi==2024.2.2

# Neo4j
neo4j==5.15.0

# Parsing & embeddings
# Use CPU-only PyTorch (much smaller image, ~2GB vs ~8GB with CUDA)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
sentence-transformers==2.2.2
huggingface-hub==0.15.1
numpy==1.24.4
tree-sitter==0.21.3
tree-sitter-python==0.21.0
tree-sitter-javascript==0.21.0
tree-sitter-typescript==0.21.0

# LLM Integration
openai==1.6.1  # DeepSeek uses OpenAI-compatible API
tiktoken==0.5.2  # Token counting for cost estimation

# Redis & worker
redis==5.0.1
rq==1.15.1

# Optional (platform-specific)
# faiss-cpu>=1.7.4  # Optional: local vector index (Linux/macOS only)
# tree_sitter>=0.2.7  # Optional: Tree-sitter bindings

# Dev / testing
pytest==7.4.3

# Authentication
PyJWT==2.10.1
python-multipart==0.0.6

# Monitoring
sentry-sdk[fastapi]==2.19.0
prometheus-fastapi-instrumentator==6.1.0